default:
    host: localhost
    user: root
    password: ''
    dbname: v3_db
    prefix: m_
    driver: pdo_mysql
    driverOptions: { 1002: 'SET NAMES utf8' }
session:
    host: localhost
    user: root
    password: null
    dbname: v3_db
    prefix: m_
    driver: pdo_mysql
    driverOptions: { 1002: 'SET NAMES utf8' }
online:
    host: ns5.mng.cc
    user: v3_user
    password: userpass
    dbname: v3_db
    prefix: m_
    driver: pdo_mysql
    driverOptions: { 1002: 'SET NAMES utf8' }
v3:
    host: localhost
    user: v3_user2
    password: userpass
    dbname: v3_db2
    prefix: c_
    driver: pdo_mysql
    driverOptions: { 1002: 'SET NAMES utf8' }
cli:
    host: localhost
    user: v3_user
    password: userpass
    dbname: v3_db
    prefix: m_
    driver: pdo_mysql
    driverOptions: { 1002: 'SET NAMES utf8' }
